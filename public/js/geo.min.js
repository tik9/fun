
// client()
async function client() {
    var res = await (await fetch('/.netlify/functions/client')).json()
    // console.log(res)
    var { ip, hostname, loc, postal, org, date, ...res } = res
    helper('info_' + arguments.callee.name, 'client').append(list(res, arguments.callee.name))

}