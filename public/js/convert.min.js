
testConversion(6, 5)
function testConversion(feet, inches) {
    const cmTotal = feet * 30.48 + inches * 2.54;

    const feetNew = Math.floor(cmTotal / 30.48);
    const inchesNew = (cmTotal - feetNew * 30.48) * 0.393701;

    // console.log(cmTotal)
};